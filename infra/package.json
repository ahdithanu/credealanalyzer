{
  "name": "cre-analyzer-infra",
  "version": "1.0.0",
  "private": true,
  "description": "AWS infrastructure for the CRE Deal Analyzer, as CDK.",
  "bin": { "infra": "bin/app.js" },
  "scripts": {
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "aws-cdk": "^2.170.0"
  }
}
